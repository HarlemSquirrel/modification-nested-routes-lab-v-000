<%= form_for @song  do |f| %>
  <%= f.label :title %>
  <%= f.text_field :title %><br>

  <%= artist_id_field(@song) %>

  <%= f.label :artist_name %>
  <%= artist_select(@song, @artist) %>
  <%= f.text_field :artist_name %>
  <br />

  <%= f.submit %>
<% end %>
